project: unishox2
# Target name preserved from the original mayhemheroes integration (unicode_fuzzer) so the
# Mayhem run history stays continuous; the binary is the ASan+UBSan decompression harness.
target: unicode_fuzzer

cmds:
  - cmd: /mayhem/unishox2-decompress-fuzz
    libfuzzer: true
